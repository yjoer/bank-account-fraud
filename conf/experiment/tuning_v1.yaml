# @package _global_
defaults:
  - _default

mlflow:
  experiment_name: clf_tuning/tuning_v1
sampling:
  variant: 2
  negative_downsampling_factor: 5
preprocessing:
  variant: 6
resampling:
  variant: 0
tuning:
  models:
    knn: true
