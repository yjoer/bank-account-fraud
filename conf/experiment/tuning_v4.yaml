# @package _global_
defaults:
  - _default

mlflow:
  experiment_name: clf_tuning/tuning_v4
sampling:
  variant: 2
  negative_downsampling_factor: 5
preprocessing:
  variant: 5
resampling:
  variant: 0
tuning:
  models:
    linear_svc: true
